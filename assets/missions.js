/* Real missionary profiles come from Supabase and are managed in Studio.
   This fallback intentionally stays empty so fictional people can never
   appear if the database is unavailable. missions-ui.js shows a temporary
   service message until the live profiles can be loaded again. */
window.FBT_MISSIONARIES = [];
